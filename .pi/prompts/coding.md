---
description: Run the Pi-native TDD coding phase
---

Use `evidence_workflow_phase_prompt` with phase `coding`, then execute the returned instructions end-to-end. Create real `src/` and `tests/` files, write audit notes under `artifacts/05-code/`, run tests if possible, then call `evidence_workflow_complete_phase`.
