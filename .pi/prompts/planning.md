---
description: Run the Pi-native Scrum planning phase
---

Use `evidence_workflow_phase_prompt` with phase `planning`, then execute the returned instructions end-to-end. Write backlog, sprint plan, Sprint 1 backlog, and DoD, then call `evidence_workflow_complete_phase`.
