---
description: Run the Evidence Workflow DDD domain modeling phase
---

Use `evidence_workflow_phase_prompt` with phase `domain_model`, then execute the returned instructions end-to-end. Write all required DDD artifacts and call `evidence_workflow_complete_phase` when done.
