---
description: Run the Evidence Workflow architecture phase
---

Use `evidence_workflow_phase_prompt` with phase `architecture`, then execute the returned instructions end-to-end. Reuse canonical knowledge from `docs/architecture/`, `contracts/`, and `engineering/evidence-workflow/`; write only iteration decisions, deltas, and `scenario-context-map.json`, then call `evidence_workflow_complete_phase`.
