---
description: Run the Pi-native architecture phase
---

Use `evidence_workflow_phase_prompt` with phase `architecture`, then execute the returned instructions end-to-end. Write architecture artifacts, API contracts, and data model, then call `evidence_workflow_complete_phase`.
